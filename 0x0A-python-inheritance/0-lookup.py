#!/usr/bin/python3
"""
This is the "0-lookup" module.
This module supplies one function, lookup().

"""


def lookup(obj):
    """
    Function that adds 2 integers.
    """
    if obj is not None:
        return dir(obj)
